import { Outlet } from "react-router"
import LandingNav from "../components/LandingNav"

function Teams() {
    return (
        <div>
            <LandingNav/>
            teams
        </div>
    )
}

export default Teams
